name: zb-cailapub

entryPoint: main.sc

botEngine: v2
language: ru

tests:
  include:
    #- general.xml
    - card.xml
    - noMatch.xml

nlp:
  intentNoMatchThresholds:
    phrases: 0.2
    patterns: 0.2
