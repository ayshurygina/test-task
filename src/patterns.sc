patterns:
    $changeInf = (изменить/поменять/сменить)
    $correctInf = (исправить/скорректировать)
    $forget = (забыл*/не помню/(не (могу/получается/выходит)/никак не) вспомнить)
    
    
    $pin = (пин/пин-код*/пинкод*/pin/pin-код*/pinкод*)
    $code = ~код
    $password = ~пароль
    
    $app = ~приложение
    $card = (~карта/карточк*)
    
    $threeWords = $oneWord [$oneWord [$oneWord]]